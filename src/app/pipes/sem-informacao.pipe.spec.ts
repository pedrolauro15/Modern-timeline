import { SemInformacaoPipe } from './sem-informacao.pipe';

describe('SemInformacaoPipe', () => {
  it('create an instance', () => {
    const pipe = new SemInformacaoPipe();
    expect(pipe).toBeTruthy();
  });
});
